<Window x:Class="UserPaymentSystem.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="User Payment System" 
        Height="600" Width="900"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Text="User Payment Management" 
                   FontSize="20" 
                   FontWeight="Bold" 
                   Margin="5"/>

        <!-- Основной контент -->
        <TabControl Grid.Row="1" Margin="0,10">
            <!-- Вкладка Пользователи -->
            <TabItem Header="Users">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Кнопки управления с привязкой команд -->
                    <StackPanel Orientation="Horizontal" Margin="5">
                        <Button Content="Add User" Width="80" Margin="5" 
                                Command="{Binding AddUserCommand}"/>
                        <Button Content="Edit" Width="80" Margin="5" 
                                Command="{Binding EditUserCommand}"/>
                        <Button Content="Delete" Width="80" Margin="5" 
                                Command="{Binding DeleteUserCommand}"/>
                        <Button Content="Refresh" Width="80" Margin="5" 
                                Command="{Binding RefreshCommand}"/>
                    </StackPanel>

                    <!-- Таблица пользователей с привязкой -->
                    <DataGrid Grid.Row="1" 
                              ItemsSource="{Binding Users}"
                              SelectedItem="{Binding SelectedUser}"
                              AutoGenerateColumns="False"
                              IsReadOnly="True"
                              Margin="5">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                            <DataGridTextColumn Header="First Name" Binding="{Binding FirstName}" Width="120"/>
                            <DataGridTextColumn Header="Last Name" Binding="{Binding LastName}" Width="120"/>
                            <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="200"/>
                            <DataGridTextColumn Header="Phone" Binding="{Binding Phone}" Width="150"/>
                            <DataGridTextColumn Header="Created" Binding="{Binding CreatedAt, StringFormat={}{0:dd.MM.yyyy}}" Width="100"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <!-- Вкладка Платежи -->
            <TabItem Header="Payments">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Horizontal" Margin="5">
                        <Button Content="Add Payment" Width="100" Margin="5" 
                                Command="{Binding AddPaymentCommand}"/>
                        <Button Content="Delete" Width="80" Margin="5" 
                                Command="{Binding DeletePaymentCommand}"/>
                        <ComboBox Name="UserFilter" Width="200" Margin="5" 
                                  ItemsSource="{Binding Users}"
                                  SelectedItem="{Binding SelectedUser}"
                                  DisplayMemberPath="FullName"/>
                        <TextBlock Text="Select user to see payments" 
                                   VerticalAlignment="Center" 
                                   Margin="5"/>
                    </StackPanel>

                    <DataGrid Grid.Row="1" 
                              ItemsSource="{Binding Payments}"
                              SelectedItem="{Binding SelectedPayment}"
                              AutoGenerateColumns="False"
                              IsReadOnly="True"
                              Margin="5">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                            <DataGridTextColumn Header="User" Binding="{Binding User.FullName}" Width="150"/>
                            <DataGridTextColumn Header="Amount" Binding="{Binding Amount, StringFormat={}{0:C}}" Width="100"/>
                            <DataGridTextColumn Header="Date" Binding="{Binding Date, StringFormat={}{0:dd.MM.yyyy}}" Width="100"/>
                            <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
        </TabControl>

        <!-- Статус бар с привязкой -->
        <StatusBar Grid.Row="2">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusText}"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>